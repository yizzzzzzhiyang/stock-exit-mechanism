# ── 核心依赖 ──
numpy>=1.23.0
pandas>=1.5.0
scipy>=1.9.0
scikit-learn>=1.2.0
xgboost>=1.7.0
pyyaml>=6.0

# ── 数据获取 ──
tushare>=1.4.0

# ── 可视化（面板 + 图表） ──
streamlit>=1.30.0
plotly>=5.15.0
matplotlib>=3.5.0

# ── 辅助 ──
tqdm>=4.60.0

# ── 开发和测试（CI 也会安装） ──
pytest>=7.0.0
mypy>=1.0.0
types-PyYAML>=6.0.0
